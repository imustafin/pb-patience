deferred class
	IV_COMPONENT

inherit

	INKVIEW_EVT

	IV_RECT

feature -- Events

	do_on_pointer_down (a_x, a_y: INTEGER): BOOLEAN
		do
		end

	do_on_pointer_up (a_x, a_y: INTEGER): BOOLEAN
		do
		end

feature

	draw
		do
		end

end
